# ============================================================
# DocPilot — requirements.txt
#
# WORKFLOW:
#   1. pip install -r requirements.txt     (resolves compatible versions)
#   2. pip freeze > requirements-lock.txt  (locks exact versions after success)
#   3. Commit requirements-lock.txt to git (for exact reproduction anywhere)
#   4. On another machine: pip install -r requirements-lock.txt
# ============================================================

# Core UI
streamlit>=1.35.0

# PDF Parsing — [pdf-image] installs layout models needed for hi_res strategy
# Without this, unstructured silently falls back to "fast" and skips image extraction
unstructured[pdf,image]>=0.13.0
unstructured-inference>=0.7.0

# PDF utilities (used internally by unstructured)
pdf2image>=1.16.3
pillow>=10.0.0
pytesseract>=0.3.10
pdfminer.six>=20221105

# LangChain ecosystem — keep these on the same minor version to avoid import errors
# 0.2.x and 0.3.x both work; DO NOT mix e.g. langchain==0.2.x with langchain-core==0.3.x
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-openai>=0.1.0

# Vector Store
faiss-cpu>=1.7.4

# Reranking + Embeddings
sentence-transformers>=2.7.0

# OpenAI SDK — must be >=1.10.0 for vision (image_url) support in ChatOpenAI
openai>=1.10.0

# Utilities
python-dotenv>=1.0.0
numpy>=1.24.0
requests>=2.31.0